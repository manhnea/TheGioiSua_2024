// Script cho các chức năng tương tác (nếu cần)
document.addEventListener('DOMContentLoaded', function () {
    console.log("Trang đã tải thành công!");
    // Thêm các hiệu ứng hoặc tương tác khác tại đây
});
